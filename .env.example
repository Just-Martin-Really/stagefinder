# The only required value. Get a free key at https://api.setlist.fm/docs/1.0/index.html
SETLISTFM_API_KEY=your_setlistfm_key_here

# Optional: DB_URL / DB_USERNAME / DB_PASSWORD and the SETLISTFM_* overrides
# all have working defaults. See the README env table to change them.
